/*
** EPITECH PROJECT, 2019
** CPool_bistro-matic_2019
** File description:
** Functions to convert a number from a baseA to a baseB
*/

#include "my.h"
//#include "infinadd.h"

static char *convert_baseA_base10(char *nbr, char* baseFrom)
{
    int i = 0;
    char *res = malloc(sizeof(char) * 200);

    while (nbr[i] != '\0') {

        i++;
    }
}

char *convertbase(char *nbr, char *baseFrom, char *baseTo)
{
    // Convertion baseFrom => base10
    // Convertion base10 => baseTo
    // Prise en charge des nombres négatifs

}
